apiVersion: v1
kind: Service
metadata:
  name: kubernetes-web-service
  labels:
    app: kubernetes-web-app
spec:
  type: ClusterIP
  selector:
    app: kubernetes-web-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
